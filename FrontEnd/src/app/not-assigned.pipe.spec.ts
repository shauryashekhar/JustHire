import { NotAssignedPipe } from './not-assigned.pipe';

describe('NotAssignedPipe', () => {
  it('create an instance', () => {
    const pipe = new NotAssignedPipe();
    expect(pipe).toBeTruthy();
  });
});
